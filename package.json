{
  "name": "eng-procurement",
  "private": true,
  "type": "module",
  "description": "Engineering-procurement synergy doctrine layer. Standalone: invariants + variables -> standard -> curated catalog -> supplier truth -> procure-to-pay. Self-contained, offline gates, no external deps.",
  "scripts": {
    "standard:selftest": "tsx scripts/standard.selftest.ts",
    "catalog:selftest": "tsx scripts/catalog.selftest.ts",
    "supplier:selftest": "tsx scripts/supplier.selftest.ts",
    "verify": "tsx scripts/verify-all.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
